<h3>PASSENGERS</h3>

<% if @passengers.length > 0 %>
  <ol>
  <% @passengers.each do |passenger| %>
    <li><%= link_to passenger.name, passenger_path(passenger.id) %> <%= passenger.phone_num %></li>
  <% end %>
  </ol>
<% else %>
    <p>No passengers in the system!</p>
<% end %>